<div class="flickr_pagination">
  <%= will_paginate feed_posts, :container => false %>
</div>
<% feed_posts.each do |post| %>
  <%= render "/shared/post", post: post %>
<% end %>
<div class="flickr_pagination mb-4">
  <%= will_paginate feed_posts, :container => false %>
</div>